# Barline 標註修復腳本 - 使用指南

## 概述

此腳本專門用於修復 Phase 5 數據集中的 Barline 標註問題，生成 Phase 6 修復版數據集。

**修復的問題：**

1. **barline (ID 23)**: 34% 的標註寬度 < 0.005（太細，YOLO 難以檢測）
2. **barline_double (ID 24)**: 67.8% 的標註面積 > 0.1（過大，容易重疊混淆）
3. **barline_final (ID 25)**: 95.9% 的標註面積 > 0.1（過大，虛假高精確率）

**預期改進：**

| 類別 | Phase 5 mAP50 | Phase 6 預期 | 提升幅度 |
|------|---------------|-------------|---------|
| barline | 0.201 | 0.50-0.60 | +150-200% |
| barline_double | 0.140 | 0.40-0.50 | +185-260% |
| barline_final | 0.708 | 0.70-0.75 | 穩定/微升 |

---

## 快速開始

### 方法 1: 使用一鍵腳本（推薦）

```bash
cd /home/thc1006/dev/music-app/training
./run_fix_barline.sh
```

腳本會自動：
1. 檢查虛擬環境
2. 安裝必要依賴
3. 運行測試驗證邏輯
4. 執行修復流程
5. 生成報告和可視化

### 方法 2: 手動執行

```bash
cd /home/thc1006/dev/music-app/training
source venv_yolo12/bin/activate

# 1. 運行測試（可選但建議）
python test_fix_barline.py

# 2. 執行修復
python fix_barline_annotations.py
```

---

## 輸入/輸出

### 輸入目錄
```
datasets/yolo_harmony_v2_phase5/
├── train/
│   ├── images/  (22,393 張圖片)
│   └── labels/  (22,393 個標註檔)
└── val/
    ├── images/  (2,517 張圖片)
    └── labels/  (2,517 個標註檔)
```

### 輸出目錄
```
datasets/yolo_harmony_v2_phase6_fixed/
├── train/
│   ├── images/  (22,393 張圖片 - 複製自 Phase 5)
│   └── labels/  (22,393 個標註檔 - 已修復)
├── val/
│   ├── images/  (2,517 張圖片 - 複製自 Phase 5)
│   └── labels/  (2,517 個標註檔 - 已修復)
├── fix_report.txt              # 詳細修復報告
├── fix_comparison.png          # 修復前後對比圖
├── distribution_comparison.png # 寬度/面積分佈對比圖
└── harmony_phase6_fixed.yaml   # YOLO 訓練配置
```

---

## 修復邏輯詳解

### 1. barline (ID 23) - 極細線擴大

**問題**：34% 寬度 < 0.005，YOLO 檢測困難

**修復策略**：
- 檢測寬度 < 0.01 的 barline
- 擴大寬度到最小 0.015（保持中心點不變）
- 確保不超出圖片邊界 [0, 1]

**示例**：
```
修復前: x=0.5, width=0.002, height=0.3, area=0.0006
修復後: x=0.5, width=0.015, height=0.3, area=0.0045  (+650% 面積)
```

### 2. barline_double/final (ID 24, 25) - 過大框緊縮

**問題**：67-95% 面積 > 0.1，容易與其他類別重疊

**修復策略**：基於寬高比智能緊縮

#### 垂直主導 (aspect_ratio > 5, barline 正常情況)
- 保留高度，緊縮寬度到合理值（約高度的 1/15）
- 目標：寬度約 0.01-0.02

#### 水平主導 (aspect_ratio < 0.2, 異常情況)
- 保留寬度，緊縮高度
- 這種情況罕見，可能是錯誤標註

#### 接近正方形 (0.2 ≤ aspect_ratio ≤ 5)
- 等比縮小到面積 ≤ 0.05
- 使用 `sqrt(0.05/原面積)` 作為縮放因子

**示例**：
```
修復前: x=0.5, y=0.5, width=0.4, height=0.5, area=0.2, ratio=1.25
修復後: x=0.5, y=0.5, width=0.2, height=0.25, area=0.05, ratio=1.25  (-75% 面積)
```

---

## 生成的報告說明

### 1. fix_report.txt

包含三大部分：

#### 修復前統計
- 每個類別的寬度、高度、面積分佈
- 極細線/過大框的數量和百分比

#### 修復後統計
- 修復後的相同統計指標
- 用於對比改進效果

#### 修復摘要
- 修復類型統計（擴大寬度、緊縮面積、無修改）
- 總修復數量

**示例輸出**：
```
【barline (ID 23)】
  總數: 25,958
  修復前:
    ⚠️ 極細線（寬度 < 0.01）: 8,933 (34.4%)
  修復後:
    ✅ 極細線（寬度 < 0.01）: 0 (0.0%)

  改進: -34.4% 極細線，成功修復 8,933 個標註
```

### 2. fix_comparison.png

視覺化修復前後對比：
- 上半部：10 個「擴大寬度」案例
- 下半部：10 個「緊縮面積」案例
- 紅色框：修復前
- 綠色框：修復後

### 3. distribution_comparison.png

分佈對比圖（2x3 網格）：
- 第 1 行：3 個類別的寬度分佈
- 第 2 行：3 個類別的面積分佈
- 紅色：修復前
- 綠色：修復後

---

## 檢查修復結果

### 1. 查看報告摘要

```bash
cat datasets/yolo_harmony_v2_phase6_fixed/fix_report.txt | grep "修復摘要" -A 10
```

### 2. 查看可視化對比

```bash
# 在本地查看（如果有 GUI）
eog datasets/yolo_harmony_v2_phase6_fixed/fix_comparison.png
eog datasets/yolo_harmony_v2_phase6_fixed/distribution_comparison.png

# 或複製到本地電腦查看
scp user@server:/path/to/datasets/yolo_harmony_v2_phase6_fixed/*.png ./
```

### 3. 驗證標註完整性

```bash
# 檢查圖片和標註數量是否一致
echo "Train images: $(ls datasets/yolo_harmony_v2_phase6_fixed/train/images/*.png | wc -l)"
echo "Train labels: $(ls datasets/yolo_harmony_v2_phase6_fixed/train/labels/*.txt | wc -l)"
echo "Val images: $(ls datasets/yolo_harmony_v2_phase6_fixed/val/images/*.png | wc -l)"
echo "Val labels: $(ls datasets/yolo_harmony_v2_phase6_fixed/val/labels/*.txt | wc -l)"
```

應該輸出：
```
Train images: 22393
Train labels: 22393
Val images: 2517
Val labels: 2517
```

### 4. 抽樣檢查修復效果

```bash
# 隨機查看一個修復前後的標註
SAMPLE=$(ls datasets/yolo_harmony_v2_phase5/train/labels/*.txt | shuf -n 1 | xargs basename)
echo "修復前:"
grep "^23 " datasets/yolo_harmony_v2_phase5/train/labels/$SAMPLE | head -3
echo "修復後:"
grep "^23 " datasets/yolo_harmony_v2_phase6_fixed/train/labels/$SAMPLE | head -3
```

---

## 下一步：Phase 6 訓練

修復完成後，使用修復後的數據集進行訓練：

### 1. 創建 Phase 6 訓練腳本

複製並修改 Phase 5 訓練腳本：

```python
# yolo12_train_phase6.py
from ultralytics import YOLO

model = YOLO('harmony_omr_v2_phase5/external_data_training/weights/best.pt')  # 從 Phase 5 繼續

results = model.train(
    data='datasets/yolo_harmony_v2_phase6_fixed/harmony_phase6_fixed.yaml',
    epochs=200,
    imgsz=640,
    batch=16,
    patience=30,
    device=0,
    workers=8,
    project='harmony_omr_v2_phase6',
    name='barline_fixed_training',
    cache=True,

    # Phase 6 特殊配置：增加 barline 類別權重
    cls_loss_weight=0.5,  # 類別損失權重
    box_loss_weight=7.5,  # 邊界框損失權重（對細線很重要）
)
```

### 2. 啟動訓練

```bash
cd /home/thc1006/dev/music-app/training
source venv_yolo12/bin/activate
python yolo12_train_phase6.py
```

### 3. 預期訓練時間（RTX 5090）

- 約 4-6 小時（200 epochs）
- 建議使用 `tmux` 或 `screen` 後台運行

### 4. 監控訓練

```bash
# 實時查看日誌
tail -f harmony_omr_v2_phase6/barline_fixed_training/train/logs.txt

# TensorBoard（可選）
tensorboard --logdir harmony_omr_v2_phase6
```

---

## 預期改進效果

基於修復後的數據集，預期 Phase 6 訓練結果：

| 指標 | Phase 5 | Phase 6 預期 | 改進幅度 |
|------|---------|-------------|---------|
| **barline mAP50** | 0.201 | 0.50-0.60 | +150-200% |
| **barline 召回率** | 0.090 | 0.45-0.55 | +400-500% |
| **barline_double mAP50** | 0.140 | 0.40-0.50 | +185-260% |
| **barline_final mAP50** | 0.708 | 0.70-0.75 | 穩定/+6% |
| **整體 mAP50** | 0.615 | 0.65-0.68 | +6-11% |

**關鍵改進點**：
- barline 從「基本不可用」(9% 召回) 提升至「可用」(45-55% 召回)
- barline_double 從「幾乎失敗」提升至「勉強可用」
- 整體性能突破 0.65 大關，接近生產可用水平

---

## 常見問題 (FAQ)

### Q1: 修復會改變圖片嗎？
**A**: 不會。腳本只修改標註文件（.txt），圖片直接複製，內容不變。

### Q2: 原始 Phase 5 數據會被覆蓋嗎？
**A**: 不會。所有修復輸出到獨立的 `phase6_fixed` 目錄，Phase 5 保持不變。

### Q3: 修復邏輯是否保守？
**A**: 是的。腳本採用保守策略：
- 只修復明顯問題（極細線、極大框）
- 中等大小的標註不修改
- 所有修復都保證在邊界內

### Q4: 為什麼不直接手動重新標註？
**A**:
- 數據量太大（24,910 張圖片，2,933,470 個標註）
- 批量修復可保證一致性
- 修復邏輯經過測試驗證，比人工更穩定

### Q5: 如果修復效果不理想怎麼辦？
**A**: 可以調整參數重新修復：
```python
# 在 fix_barline_annotations.py 中修改
MIN_BARLINE_WIDTH = 0.020  # 改為更寬
MAX_REASONABLE_AREA = 0.03  # 改為更緊
```

### Q6: 修復後還需要合成數據嗎？
**A**: 視訓練結果而定：
- 如果 Phase 6 達到 mAP50 > 0.6，可能不需要
- 如果 barline_double 仍 < 0.4，建議用 Abjad 生成補充數據

---

## 技術細節

### 修復參數

```python
MIN_BARLINE_WIDTH = 0.015       # barline 最小寬度
THIN_BARLINE_THRESHOLD = 0.01   # 極細線閾值
LARGE_AREA_THRESHOLD = 0.1      # 過大框閾值
MAX_REASONABLE_AREA = 0.05      # 合理最大面積
```

### 處理流程

1. **掃描階段**（約 30 秒）
   - 統計所有 barline 相關標註
   - 識別需要修復的案例

2. **修復階段**（約 2-3 分鐘）
   - 逐個處理標註文件
   - 應用修復邏輯
   - 複製圖片文件

3. **分析階段**（約 30 秒）
   - 重新統計修復後數據
   - 對比修復前後差異

4. **可視化階段**（約 1 分鐘）
   - 生成對比圖
   - 生成分佈圖

**總耗時**：約 5 分鐘

### 記憶體使用

- 峰值記憶體：< 2GB
- 硬碟空間需求：約 2.5GB（Phase 6 完整副本）

---

## 故障排除

### 錯誤：ModuleNotFoundError: No module named 'matplotlib'

**解決**：
```bash
source venv_yolo12/bin/activate
pip install matplotlib Pillow tqdm numpy
```

### 錯誤：Permission denied

**解決**：
```bash
chmod +x run_fix_barline.sh
```

### 錯誤：No space left on device

**解決**：
```bash
# 檢查硬碟空間
df -h

# 刪除舊的訓練日誌（如果不需要）
rm -rf harmony_omr_v2_phase*/*/train/
```

### 修復進度卡住

**解決**：
```bash
# Ctrl+C 中斷
# 檢查是否有鎖定的文件
lsof | grep phase6_fixed

# 刪除部分輸出重新開始
rm -rf datasets/yolo_harmony_v2_phase6_fixed
```

---

## 版本資訊

- **腳本版本**: 1.0
- **作者**: Claude Code
- **日期**: 2025-11-26
- **適用數據集**: yolo_harmony_v2_phase5
- **目標數據集**: yolo_harmony_v2_phase6_fixed

---

## 聯繫與支援

如有問題或建議，請：
1. 查看 `/home/thc1006/dev/music-app/training/barline_analysis_report.txt` 了解問題背景
2. 檢查修復報告 `fix_report.txt` 了解具體修復情況
3. 查看測試腳本 `test_fix_barline.py` 了解修復邏輯

---

**祝訓練順利！期待 Phase 6 的性能突破！** 🚀
